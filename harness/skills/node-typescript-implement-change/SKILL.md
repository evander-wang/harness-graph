---
name: node-typescript-implement-change
description: 按已通过的分析范围实现 Node.js TypeScript 变更。
---

# 实现 Node.js TypeScript 变更

遵守当前仓库 `AGENTS.md` 和现有代码约定，只实现已确认范围。

从 Runtime 返回的聚合 evidence 中定位 `planPath=`。存在该证据时，先完整读取计划文件，再按其中已通过的范围和“接口与文档影响”实施；简单任务没有计划文件时，确认 evidence 已记录未落盘原因。实际调查需要扩大范围或产生新的接口文档影响时，先停止实现、更新计划并向用户说明。

新增或修改公开 Interface、Schema、CLI、事件或配置契约时，先更新计划指定的机器事实源，再同步面向使用者的说明和示例；遵循项目现有文档约定，不用 Markdown 复制可由机器事实源表达的字段结构。

行为变化先增加或调整测试并确认测试因缺少目标行为而失败，再编写最小实现。优先使用 `unknown` 和类型收窄，不使用 `any` 绕过问题。

不得静默删除已有功能，不直接修改生成物，不借机进行范围外重构。输出修改摘要、变更文件和仍需验证的风险。
